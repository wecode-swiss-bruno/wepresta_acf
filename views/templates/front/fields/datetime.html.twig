{#**
 * WePresta ACF - Advanced Custom Fields for PrestaShop
 *
 * @author    WePresta
 * @copyright 2024-2025 WePresta
 * @license   https://opensource.org/licenses/AFL-3.0 Academic Free License 3.0 (AFL-3.0)
 *#}

{#
 # ACF DateTime Field Template (Twig)
 #}

{% if value is not empty %}
    {% set format = config.displayFormat|default('d/m/Y H:i') %}
    
    <time class="acf-datetime{% if customClass %} {{ customClass }}{% endif %}" 
          datetime="{{ value }}"
          {% if customId %}id="{{ customId }}"{% endif %}>
        {{ value|date(format) }}
    </time>
{% endif %}
